import theme from "styled-theming"

export const sidebarBackgroundColor = theme("mode", {
  light: "#343434",
  dark: "#343434",
})

export const sidebarForegroundColor = theme("mode", {
  light: "#ddd",
  dark: "#eee",
})

export const menuItemPadding = "0.75rem 1rem 0.75rem 1rem"
